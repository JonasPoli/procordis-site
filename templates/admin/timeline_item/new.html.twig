{% extends 'admin/base_admin.html.twig' %}

{% block page_title %}Novo Evento{% endblock %}

{% block body %}
    <div class="glass-panel text-sm px-6 py-6 rounded-xl shadow-sm">
        <div class="mb-6 flex items-center justify-between">
            <h2 class="text-lg font-bold text-gray-900 dark:text-gray-100">Novo Evento</h2>
            <a href="{{ path('admin_timeline_index') }}" class="text-slate-500 hover:text-slate-700 dark:hover:text-slate-300 transition-colors">&larr; Voltar</a>
        </div>
        
        {{ include('admin/timeline_item/_form.html.twig') }}
    </div>
{% endblock %}
