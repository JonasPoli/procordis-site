{% extends 'admin/base_admin.html.twig' %}

{% block page_title %}Nova Página{% endblock %}

{% block body %}
    <div class="mb-6">
        <a href="{{ path('admin_page_index') }}" class="text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300">
            &larr; Voltar para a lista
        </a>
    </div>

    <div class="glass-panel rounded-2xl p-6">
        <h2 class="text-xl font-bold mb-6 text-gray-800 dark:text-white">Criar Nova Página</h2>
        
        {{ include('admin/page/_form.html.twig') }}
    </div>
{% endblock %}
