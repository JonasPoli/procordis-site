{% extends 'admin/base_admin.html.twig' %}

{% block page_title %}Editar Serviço{% endblock %}

{% block body %}
    <div class="glass-panel p-6 rounded-2xl">
        <div class="mb-4">
            <a href="{{ path('admin_service_index') }}" class="text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300">&larr; Voltar para lista</a>
        </div>
        
        {{ include('admin/service/_form.html.twig', {'button_label': 'Atualizar'}) }}
    </div>
{% endblock %}
