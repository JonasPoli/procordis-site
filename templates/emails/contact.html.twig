<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>Novo Contato pelo Site</title>
		<style>
			body {
				font-family: Arial, sans-serif;
				background-color: #f4f7f6;
				color: #333333;
				margin: 0;
				padding: 0;
			}
			.container {
				width: 100%;
				max-width: 600px;
				margin: 30px auto;
				background-color: #ffffff;
				border-radius: 8px;
				overflow: hidden;
				box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
			}
			.header {
				background-color: #0E7490; /* Cyan 700 - Combina com layout médico */
				color: #ffffff;
				text-align: center;
				padding: 24px;
			}
			.header h1 {
				margin: 0;
				font-size: 24px;
				font-weight: bold;
				letter-spacing: 0.5px;
			}
			.content {
				padding: 30px;
				line-height: 1.6;
				font-size: 15px;
			}
			.info-box {
				background-color: #F8FAFC;
				border-left: 4px solid #06B6D4;
				padding: 16px 20px;
				margin-bottom: 24px;
				border-radius: 4px;
			}
			.info-row {
				margin-bottom: 8px;
			}
			.info-row:last-child {
				margin-bottom: 0;
			}
			.info-row strong {
				display: inline-block;
				width: 80px;
				color: #475569;
			}
			.message-box {
				background-color: #ffffff;
				border: 1px solid #E2E8F0;
				padding: 20px;
				border-radius: 6px;
				margin-top: 10px;
				white-space: pre-wrap;
				color: #334155;
				font-size: 15px;
			}
			.footer {
				background-color: #F1F5F9;
				color: #64748B;
				text-align: center;
				padding: 16px;
				font-size: 13px;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<div class="header">
				<h1>Associação Procordis</h1>
				<p style="margin: 8px 0 0 0; opacity: 0.85; font-size: 14px;">Você recebeu um novo contato pelo site</p>
			</div>

			<div class="content">
				<div class="info-box">
					<div class="info-row">
						<strong>Nome:</strong>
						{{ contact.name }}</div>
					<div class="info-row">
						<strong>E-mail:</strong>
						<a href="mailto:{{ contact.email }}" style="color: #0284C7; text-decoration: none;">{{ contact.email }}</a>
					</div>
					{% if contact.phone %}
						<div class="info-row">
							<strong>Tel:</strong>
							{{ contact.phone }}</div>
					{% endif %}
					<div class="info-row">
						<strong>Assunto:</strong>
						{{ contact.subject }}</div>
				</div>

				<p style="color: #0F172A; font-weight: bold; margin-bottom: 5px;">Corpo da mensagem:</p>
				<div class="message-box">{{ contact.message }}</div>
			</div>

			<div class="footer">
				<p style="margin: 0;">Este e-mail foi gerado automaticamente pelo portal da Procordis.</p>
			</div>
		</div>
	</body>
</html>
