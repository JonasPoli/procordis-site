{% extends 'base.html.twig' %}

{% block title %}Resultados da Busca: {{ query }} - Procordis{% endblock %}

{% block content %}
    <!-- Hero Small / Search Header -->
    <div class="bg-primary-900 py-16 text-white relative overflow-hidden">
         <div class="absolute inset-0 bg-[url('https://www.transparenttextures.com/patterns/medical-icons.png')] opacity-10"></div>
        <div class="container mx-auto px-6 relative z-10 text-center">
            <h1 class="text-3xl md:text-5xl font-display font-bold mb-4">Resultados da Busca</h1>
            {% if query %}
                <p class="text-primary-200 text-lg">Exibindo resultados para: <span class="text-white font-bold">"{{ query }}"</span></p>
            {% else %}
                 <p class="text-primary-200 text-lg">Digite algo para pesquisar.</p>
            {% endif %}
        </div>
    </div>

    <!-- Search Bar/Form -->
    <div class="bg-white border-b border-slate-100 sticky top-[80px] z-30 shadow-sm">
        <div class="container mx-auto px-6 py-4">
             <form action="{{ path('app_search') }}" method="get" class="max-w-2xl mx-auto relative">
                <input type="text" name="q" value="{{ query }}" placeholder="Pesquisar por serviços, notícias, médicos..." class="w-full pl-5 pr-14 py-3 rounded-full border border-slate-200 focus:outline-none focus:border-primary-500 focus:ring-1 focus:ring-primary-500 shadow-sm text-slate-700 placeholder-slate-400">
                <button type="submit" class="absolute right-2 top-1/2 transform -translate-y-1/2 w-10 h-10 bg-primary-600 text-white rounded-full flex items-center justify-center hover:bg-primary-700 transition-colors">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                </button>
            </form>
        </div>
    </div>

    <!-- Results -->
    <section class="py-16 bg-slate-50 min-h-[50vh]">
        <div class="container mx-auto px-6">
            
            {% if news is empty and services is empty and query %}
                <div class="text-center py-20">
                    <div class="w-20 h-20 bg-slate-200 rounded-full flex items-center justify-center mx-auto text-slate-400 mb-6">
                        <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                    </div>
                    <h3 class="text-2xl font-bold text-slate-900 mb-2">Nenhum resultado encontrado</h3>
                    <p class="text-slate-500">Tente buscar por outros termos ou verifique a ortografia do que digitou.<br>Você pode tentar termos como "Cardiologia", "Exames" ou "Horários".</p>
                </div>
            {% elseif not query %}
                 <div class="text-center py-10 opacity-50">
                     <p>Use a barra de pesquisa acima para encontrar o que procura.</p>
                 </div>
            {% else %}
            
                <div class="flex flex-col lg:flex-row gap-12">
                    <!-- Services Results -->
                    <div class="lg:w-1/2">
                        <h2 class="text-xl font-bold text-slate-900 mb-6 flex items-center">
                            <span class="w-8 h-8 bg-blue-100 text-blue-600 rounded-lg flex items-center justify-center mr-3 text-sm"><i class="fas fa-stethoscope"></i></span>
                            Serviços e Exames ({{ services|length }})
                        </h2>
                        
                        {% if services is not empty %}
                            <div class="space-y-4">
                                {% for service in services %}
                                    <a href="{{ path('app_service_show', {id: service.id}) }}" class="block bg-white p-6 rounded-xl shadow-sm border border-slate-100 hover:shadow-md hover:border-blue-200 transition-all group">
                                        <h3 class="text-lg font-bold text-slate-900 group-hover:text-blue-600 transition-colors mb-2">{{ service.title }}</h3>
                                        <p class="text-slate-500 text-sm line-clamp-2">{{ service.description }}</p>
                                    </a>
                                {% endfor %}
                            </div>
                        {% else %}
                             <p class="text-slate-400 italic text-sm">Nenhum serviço encontrado.</p>
                        {% endif %}
                    </div>

                    <!-- News Results -->
                    <div class="lg:w-1/2">
                         <h2 class="text-xl font-bold text-slate-900 mb-6 flex items-center">
                            <span class="w-8 h-8 bg-green-100 text-green-600 rounded-lg flex items-center justify-center mr-3 text-sm"><i class="far fa-newspaper"></i></span>
                            Notícias e Artigos ({{ news|length }})
                        </h2>

                        {% if news is not empty %}
                             <div class="space-y-4">
                                {% for item in news %}
                                     <a href="{{ path('app_news_show', {slug: item.slug}) }}" class="flex gap-4 bg-white p-4 rounded-xl shadow-sm border border-slate-100 hover:shadow-md hover:border-green-200 transition-all group">
                                        {% if item.imageName %}
                                           <div class="w-24 h-24 rounded-lg overflow-hidden flex-shrink-0">
                                                <img src="{{ asset('images/news/' ~ item.imageName) }}" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500">
                                           </div> 
                                        {% endif %}
                                        <div>
                                            <h3 class="font-bold text-slate-900 group-hover:text-green-600 transition-colors mb-1">{{ item.title }}</h3>
                                            <p class="text-xs text-slate-400 mb-2">{{ item.publishedAt|date('d/m/Y') }}</p>
                                            <p class="text-slate-500 text-sm line-clamp-2">{{ item.summary }}</p>
                                        </div>
                                    </a>
                                {% endfor %}
                            </div>
                        {% else %}
                            <p class="text-slate-400 italic text-sm">Nenhuma notícia encontrada.</p>
                        {% endif %}
                    </div>
                </div>

            {% endif %}
        </div>
    </section>
{% endblock %}
