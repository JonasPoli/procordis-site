{% for service in services %}
    <li>
        <a href="{{ path('app_service_show', {slug: service.slug}) }}" class="text-primary-foreground/70 hover:text-white hover:translate-x-1 transition-all duration-300 text-sm block">
            {{ service.title }}
        </a>
    </li>
{% endfor %}
