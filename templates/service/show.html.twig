{% extends 'base.html.twig' %}

{% block title %}{{ service.title }} - Procordis{% endblock %}
{% block description %}{{ service.description }}{% endblock %}

{% block content %}
    <!-- Hero Small / Breadcrumb -->
    <div class="bg-primary-900 py-20 text-white relative overflow-hidden">
         <div class="absolute inset-0 bg-[url('https://www.transparenttextures.com/patterns/medical-icons.png')] opacity-10"></div>
        <div class="container mx-auto px-6 relative z-10">
            <a href="{{ path('app_services') }}" class="inline-flex items-center text-primary-200 hover:text-white transition-colors mb-4 text-sm font-bold uppercase tracking-wider group">
                <svg class="w-4 h-4 mr-2 group-hover:-translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
                Voltar para Serviços
            </a>
            <h1 class="text-3xl md:text-5xl font-display font-bold">{{ service.title }}</h1>
        </div>
    </div>

    <!-- Content -->
    <section class="py-20 bg-white">
        <div class="container mx-auto px-6">
            <div class="flex flex-col lg:flex-row gap-16">
                <!-- Main Content -->
                <div class="lg:w-2/3">
                    {% if service.imageName %}
                        <div class="rounded-2xl overflow-hidden shadow-2xl mb-10 border border-slate-100">
                             <img src="{{ asset('images/services/' ~ service.imageName) }}" alt="{{ service.title }}" class="w-full">
                        </div>
                    {% endif %}

                    <div class="prose prose-lg prose-slate prose-headings:font-display prose-headings:text-slate-900 prose-a:text-primary-600 max-w-none text-slate-700 leading-relaxed">
                        {{ service.content|raw }}
                    </div>
                </div>

                <!-- Sidebar -->
                <div class="lg:w-1/3">
                    <div class="bg-slate-50 p-8 rounded-2xl border border-slate-100 sticky top-28 shadow-lg">
                        <div class="w-12 h-12 bg-primary-100 text-primary-600 rounded-xl flex items-center justify-center mb-6">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
                        </div>
                        <h3 class="text-2xl font-display font-bold text-slate-900 mb-2">Agende seu exame</h3>
                        <p class="text-slate-600 mb-8 leading-relaxed">Entre em contato para agendar este procedimento. Nossa equipe está pronta para atendê-lo com rapidez.</p>
                        
                         <a href="https://wa.me/{{ templateService.generalData.whatsapp|default('')|replace({' ': '', '-': '', '(': '', ')': ''}) }}" target="_blank" class="flex items-center justify-center w-full px-6 py-4 bg-[#25D366] text-white font-bold rounded-xl shadow-lg hover:shadow-green-500/30 hover:bg-[#20bd5a] transition-all duration-300 mb-4 transform hover:-translate-y-1">
                            <i class="fab fa-whatsapp text-xl mr-3"></i>
                            Agendar pelo WhatsApp
                        </a>
                         <a href="{{ path('app_contact') }}" class="flex items-center justify-center w-full px-6 py-4 bg-white border border-primary-600 text-primary-600 font-bold rounded-xl hover:bg-primary-50 transition duration-300">
                            Ver Telefones e Endereço
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
